# tst/unit/api/

Unit tests for `src/trading/api/`.

## Sub-directories

| Directory | Tests for |
|-----------|---------|
| `dashboard/` | FastAPI endpoints + `DashboardServer` component lifecycle |

Telegram alerter tests live in `tst/unit/monitoring/` (alongside `HeartbeatMonitor`, which owns the alerter callback).
